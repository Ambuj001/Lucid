version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: cardwise
      POSTGRES_PASSWORD: cardwise123
      POSTGRES_DB: cardwisedb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
